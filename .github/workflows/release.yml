name: Release

on:
  release:
    types: [published]

jobs:
  verify:
    uses: ./.github/workflows/verify.yml
    secrets: inherit
  
  publish:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Publish to npm
        run: npm publish